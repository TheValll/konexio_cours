const country = "France";
let population = 68; // en millions
const averagePopulation = 33; // en millions

// population >= averagePopulation ? "above" : "below";

console.log(
  `${country} population is ${
    population >= averagePopulation ? "above" : "below"
  } the average`
);
